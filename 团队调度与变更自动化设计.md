# 团队调度、审核与变更自动化设计

## 1. 结论：建立一个“研发控制塔”

对 10 人团队，最实用的做法不是再增加一个项目管理工具，而是把飞书多维表格作为研发控制塔：

```text
客户/销售/PM 提交变更
          ↓
飞书需求/变更主记录（唯一事件源）
          ↓
规则计算责任人、依赖、订阅人、需重开的门禁
          ↓
创建任务 + 通知 + 审核 + SLA 计时
          ↓
TRAE 读取上下文、调用 Skill 做影响分析/产物更新
          ↓
Git/CI/CD/Figma/飞书返回真实证据
          ↓
控制塔聚合状态并继续驱动下一节点
```

飞书负责“事件、任务、人员、审批和通知”；TRAE 负责“理解、分析、生成和执行”；Git/CI/CD/Figma 负责各自领域的真实状态。

重要边界：TRAE 企业 Hook 是智能体会话生命周期 Hook，不是需求系统的事件总线。客户需求变化应由飞书自动化、飞书事件订阅或内部工作流服务触发；TRAE Hook 用于给 AI 注入最新上下文、校验输出、拦截危险操作和通知任务完成。

## 2. 控制塔的数据模型

建议不要把所有信息放在一张大表里。建立 6 张关联表。

### 2.1 需求表 `Requirements`

| 字段 | 示例 | 说明 |
|---|---|---|
| REQ-ID | REQ-2026-042 | 全链路唯一编号 |
| 标题 | 批量导出订单 | 简短业务标题 |
| DRI | 王敏 | 对最终交付负责的人，只能有 1 个 |
| 当前阶段 | In Dev | 从状态机选择，不手填自由文本 |
| 健康度 | Yellow | 由阻塞、延期、门禁失败自动计算 |
| 优先级 | P1 | 业务优先级 |
| 风险等级 | High | 决定审核人与通知范围 |
| 目标版本 | v2.8.0 | 关联发布计划 |
| 当前版本基线 | PRD v1.0/TSD v1.0 | 变更影响基准 |
| 下一门禁 | Code Review | 由当前阶段计算 |
| 下个动作 Owner | 张强 | 当前最需要行动的人 |
| 到期时间 | 2026-09-18 18:00 | 下一动作 SLA，而非模糊项目日期 |
| PRD/Figma/TSD/PR/Test/Release | 链接 | 证据链 |

### 2.2 工作项表 `Work Items`

一条记录对应一个人可以独立完成并验收的任务。

| 字段 | 示例 |
|---|---|
| TASK-ID | TASK-2026-042-07 |
| REQ-ID | REQ-2026-042 |
| 类型 | Backend / Design / QA / Review / Release |
| 标题 | 实现导出任务 API |
| Owner | 张强 |
| Reviewer | 陈涛 |
| 状态 | Ready / Doing / In Review / Blocked / Done |
| 输入版本 | PRD v1.0, TSD v1.0 |
| 输出/证据 | PR #231 |
| 前置依赖 | TASK-2026-042-03 |
| 开始/到期/SLA | 日期时间 |
| Blocked Reason | 等待安全评审 |
| 更新时间 | 自动时间 |

### 2.3 产物表 `Artifacts`

| 字段 | 示例 |
|---|---|
| ART-ID | ART-REQ-2026-042-TSD |
| 类型 | PRD/TSD/Figma/API/Test/Runbook |
| Owner | 陈涛 |
| 当前版本 | v1.1 |
| 状态 | Draft/Review/Approved/Stale/Archived |
| 权威链接 | 飞书/Figma/Git 链接 |
| 依赖 FR-ID | FR-01, FR-03 |
| 依赖上游 ART-ID | PRD、Figma |
| 最后审核时间 | 日期 |

`Stale` 是关键状态：上游变更后，不立即假设下游文档仍有效，而是自动标记待复核。

### 2.4 依赖与订阅表 `Dependencies`

| From | To | 关系 | 自动订阅人 |
|---|---|---|---|
| PRD FR-03 | Figma ExportDialog | implements | 设计师、前端 |
| PRD FR-03 | TSD ExportJob API | implements | TL、后端 |
| TSD ExportJob API | PR #231 | implemented-by | PR Owner/Reviewer |
| PR #231 | Test TC-042-12 | verified-by | QA |
| Release v2.8.0 | Runbook Export | operated-by | SRE |

没有这张关系表，就只能“@所有人”；有了它，系统才能精确通知受影响的人。

### 2.5 变更表 `Changes`

| 字段 | 示例 |
|---|---|
| Change-ID | CR-2026-042-01 |
| REQ-ID | REQ-2026-042 |
| 来源 | 客户 A / 销售李明 |
| 原基线 | PRD v1.0 |
| 变更内容 | 导出增加客户手机号 |
| 原因/价值 | 客服批量回访 |
| 紧急度 | Normal/Expedite |
| 影响范围 | AI 草稿 + 人工确认 |
| 决策 | Assessing/Approved/Rejected/Deferred |
| 批准人 | PM/TL/QA/安全/客户代表 |
| 生效版本 | PRD v1.1 |
| 确认率 | 4/5 |

### 2.6 审核表 `Approvals`

不要用一个“已审核”勾选框。每个审核人一条记录：

| Gate-ID | 对象 | Role | Approver | Decision | Comment | Time |
|---|---|---|---|---|---|---|
| GATE-042-PRD | PRD v1.1 | Product | 王敏 | Approved | — | 10:30 |
| GATE-042-PRD | PRD v1.1 | Tech | 陈涛 | Changes requested | 权限未定义 | 10:45 |

最终门禁状态由所有必需审核记录自动聚合；AI 不得写入真人的 `Decision`。

### 2.7 人员目录 `People`

要把流程真正关联到人，不能用姓名作为系统主键。建立统一 `Person-ID`，把成员在各系统中的账号映射起来：

| 字段 | 示例 | 用途 |
|---|---|---|
| Person-ID | P-0007 | 永久主键；人员改名不影响历史记录 |
| 姓名 | 张强 | 展示 |
| 在职状态 | Active | 离职/休假时停止自动派单 |
| 飞书人员字段/用户 ID | 张强对应的飞书用户 | 私聊、任务、日程、审批的实际接收者 |
| 企业邮箱 | zhangqiang@example.com | 对齐 TRAE 企业 Hook 中的成员邮箱 |
| TRAE 企业账号邮箱 | zhangqiang@example.com | 识别谁运行了智能体任务 |
| Git 账号 | zhangqiang-dev | 识别 PR Author/Reviewer |
| 团队角色 | Backend | 默认审核和通知规则 |
| 负责组件 | order-service | 自动派单和影响计算 |
| 专长标签 | PII、MySQL | 高风险变更加签 |
| Backup | P-0003 | 休假或超时自动改派/升级 |
| Manager/Escalation | P-0001 | SLA 超期升级 |
| 时区/工作时间 | Asia/Shanghai | 避免非紧急夜间提醒 |
| 通知偏好 | Action=即时，FYI=日报 | 控制通知噪音 |

建议直接使用飞书“人员”类型字段关联成员，而不是手工输入 open_id。若另外开发工作流服务，再在服务端安全地维护飞书用户 ID、Git 账号和 Person-ID 的映射。

人员入职、转组、休假、离职时只更新 People/Ownership 两处。历史任务仍保留原 Owner，新任务根据 `Active`、Backup 和组件责任重新路由。

### 2.8 通知队列表 `Notifications`

把“需要通知”也作为可审计记录，不要让各自动化流程直接随意发消息：

| 字段 | 示例 |
|---|---|
| Notification-ID | N-2026-0911-0082 |
| Event-ID | EVT-CR-042-01 |
| Recipient | P-0007 张强 |
| 类型 | Action / Review / FYI / Escalation |
| 原因 | order-service Owner |
| 行动 | 确认 API/PII 影响 |
| 对象链接 | TASK/CR/PR 链接 |
| 到期时间 | 2026-09-11 15:00 |
| 状态 | Pending/Sent/Acknowledged/Failed/Expired |
| 发送渠道 | 飞书私聊/任务/群/日报 |
| 发送次数/最后发送 | 1 / 10:05 |

同一 `Event-ID + Recipient + 类型` 只允许一条有效通知，避免重复提醒。工作流先生成通知队列，再由发送器投递、重试和记录结果。

### 2.9 事件发生后如何算出具体的人

统一采用以下解析顺序：

```text
事件（需求/变更/PR/CI/阻塞/发布）
  → 找直接责任：DRI、当前 TASK Owner、Reviewer
  → 找对象标签：组件、FR-ID、风险、环境
  → 查 Ownership：组件 Owner、领域 Reviewer、QA、Backup
  → 查 Dependencies：所有受影响 Artifact Owner 和下游 Task Owner
  → 查风险规则：安全、隐私、数据、SRE 等必需审核角色
  → 查订阅：客户负责人、销售、管理层等 FYI 人员
  → 排除离职/休假、替换 Backup、去重
  → 分为 Action / Review / FYI / Escalation
  → 生成 Notifications 记录并发送
```

一个人可能因为多个原因被命中，只发送一条消息，但在“通知原因”中列全。例如张强同时是 `order-service Owner` 和当前任务 Owner，只收到一条 Action 通知。

### 2.10 主动通知的渠道规则

| 通知类型 | 默认渠道 | 是否要求确认 | 例子 |
|---|---|---:|---|
| Action | 飞书任务 + 私聊卡片 | 是 | 评估变更、修改 TSD、解除阻塞 |
| Review | 飞书审批/任务 + 私聊 | 是 | PRD/TSD/PR/发布审核 |
| FYI | 项目群摘要或每日摘要 | 否 | 排期变化、已批准变更 |
| Escalation | 私聊责任人和上级，必要时项目群 | 是 | SLA 超期、P0、发布失败 |
| Emergency | 私聊+应急群+电话值班系统 | 是 | 线上 P0/P1 |

主动通知采用“即时行动、批量知会”：需要动作或审核的立即发；普通 FYI 汇总到日报。禁止所有事件都 @全员。

确认不能以“看过消息”代替。接收人必须在任务/审核记录中点击或选择：`接受`、`需澄清`、`无法承担/请求改派`、`批准`、`要求修改`。

### 2.11 人员关联的完整例子

客户提交 `CR-2026-042-01：导出新增手机号`，记录标签为：

```text
REQ-ID=REQ-2026-042
FR-ID=FR-03
Components=admin-web, order-service, customer-pii
Risk=High
```

系统解析得到：

| 人 | 命中规则 | 通知级别 | 自动创建的行动 |
|---|---|---|---|
| 王敏/PM | REQ DRI | Action | 完成客户澄清、提交变更审批 |
| 李婷/设计 | admin-web 订阅人、Figma Owner | Action | 判断是否需要字段选择交互 |
| 张强/后端 | order-service Owner、TASK Owner | Action | 评估接口、脱敏和工期 |
| 陈涛/TL | order-service Reviewer、High Risk Approver | Review | 审核技术影响和 TSD |
| 赵雪/QA | 默认 QA、测试产物 Owner | Action | 更新权限/脱敏/回归用例 |
| 刘洋/安全 | customer-pii Reviewer | Review | 审核最小权限、脱敏与审计 |
| 周宁/SRE | order-service FYI、Runbook Owner | FYI；批准后转 Action | 评估监控和发布影响 |
| 李明/销售 | Change 来源/客户接口人 | FYI | 接收内部结论和回复草稿 |

未受影响的移动端开发和其他后端不收到即时消息。

发送给张强的消息：

```text
[需行动｜今天 15:00 前确认]
CR-2026-042-01：订单导出新增客户手机号

通知你的原因：
- 你是 order-service 组件 Owner
- 你负责正在进行的 TASK-2026-042-07
- FR-03/API/PII 处理可能受到影响

请在任务中选择：
1. No impact（说明原因）
2. Update required（填写影响、估算、依赖）
3. Need clarification（填写问题和回答人）
4. Request reassignment（选择原因）

变更记录：[链接]
当前基线：PRD v1.0 / TSD v1.0
逾期升级：TL 陈涛
```

如果张强 12:00 前没有确认，发送一次温和提醒；15:00 到期后，Notifications 创建 Escalation 记录，同时通知张强和陈涛。若张强处于休假状态，则初次分派时直接使用 Backup，不等到超时。

### 2.12 在 TRAE 中怎样操作

TraeWork 已授权飞书后，可以查找用户、读取/管理任务、发送消息、管理审批和更新多维表格。推荐让 DRI 使用以下指令生成并确认通知：

```text
处理事件 CR-2026-042-01。

读取飞书中的 People、Ownership、Requirements、Work Items、Artifacts、Dependencies 和 Approvals。
按“直接责任 → 组件责任 → 下游依赖 → 风险加签 → 订阅人”的顺序解析收件人。

输出收件人清单，字段包括：Person-ID、姓名、命中原因、通知级别、行动、截止时间、升级人。
合并重复收件人，排除 Inactive/Leave，并使用 Backup。
先给我审核，不发送任何消息。
```

DRI 确认名单后：

```text
收件人清单已确认。

请在 Notifications 表创建待发送记录：
- Action/Review 使用飞书任务并准备私聊消息；
- FYI 进入项目日报，不即时私聊；
- 每条记录必须写 Event-ID、原因、行动、截止时间和对象链接；
- 不要修改 Approvals.Decision，不要代替任何人确认。

创建完成后返回通知记录，不发送客户消息。
```

如果组织允许 TraeWork 发送飞书消息，再由授权成员确认执行发送。若要求事件发生后无人值守自动通知，应由飞书工作流或内部工作流服务读取 Notifications 队列发送，而不是依赖某个成员保持 TRAE 对话开启。

TRAE 企业 Hook 中包含成员 `email`，可以将正在运行任务的人映射回 People 表；`Notification` Hook 可以把“智能体完成/等待确认”等事件发送给相应成员。但它只覆盖 TRAE 任务事件，不覆盖客户在飞书中新建变更等外部事件。

## 3. 任务如何分配

### 3.1 固定 DRI，动态执行人

- 每个 REQ 只有一个 DRI，通常是 PM 或 TL。
- 每个 TASK 只有一个 Owner；协作者放 Collaborators，不允许多人共同 Owner。
- Reviewer 不得与 Owner 相同；高风险代码至少 TL 或领域 Owner 参与。
- 任务被阻塞时，Owner 不变；新增 `Unblock Owner`，避免责任消失。

### 3.2 用组件责任矩阵自动路由

建立 `Ownership` 配置：

| 组件/标签 | 默认 Owner | Reviewer | QA | 订阅人 |
|---|---|---|---|---|
| admin-web | 前端 A | 前端 B | QA | 设计、PM |
| order-service | 后端 A | TL | QA | PM、SRE |
| customer-pii | 后端 B | TL+安全 | QA | DPO/SRE |
| ci-deploy | SRE | TL | QA | 全体开发 |

需求进入 `Ready for Dev` 时，自动化根据 TSD 中的组件标签创建工作项并填充默认 Owner/Reviewer。DRI 只处理冲突和容量调整，不逐项手工找人。

### 3.3 容量与 WIP 规则

对 10 人团队建议：

- 每名开发同时 `Doing` 的功能任务不超过 2 个。
- Reviewer 的 `In Review` 不超过 3 个；超限自动提醒 TL 改派。
- QA 只接收达到 Ready for QA 门禁的制品，避免半成品反复测试。
- Expedite 通道同时最多 1 项；插队必须记录被延后的任务。

### 3.4 在 TraeWork 中生成分工草稿

DRI 输入：

```text
使用 implementation-planning 处理 REQ-2026-042。
读取飞书 Requirements、Ownership、当前 Work Items，以及批准的 PRD/TSD。

根据组件责任矩阵和当前 WIP 生成任务分配草稿：
- 每个任务只有一个 Owner 和一个 Reviewer；
- 标出依赖、关键路径、估算、输入版本、验收证据；
- 不要覆盖成员已有任务；WIP 超限时给出改派建议；
- 包含开发、设计、测试、监控、文档、发布和回滚任务。

先展示草稿。经我确认后，再新增 Work Items 记录并通知 Owner。
```

自动分配只生成草稿；涉及成员承诺和优先级冲突时由 DRI/TL 确认。

## 4. 状态如何更新

### 4.1 状态由证据驱动，不靠周报

工作项状态规则：

| 状态迁移 | 必须满足 |
|---|---|
| Ready → Doing | Owner、输入版本和验收标准已填 |
| Doing → In Review | 已有产物链接；开发任务必须有 PR |
| In Review → Done | Reviewer 批准且自动检查通过 |
| 任意 → Blocked | 原因、Unblock Owner、预计解除时间齐全 |

需求状态由工作项和门禁聚合：例如只有所有开发项 Done、PR 合并、候选制品存在，需求才可进入 `In Test`。

### 4.2 三类状态更新来源

1. **成员更新**：Owner 在 TraeWork 中让飞书插件更新自己负责的 Work Item。
2. **系统更新**：PR、CI、部署或监控通过 webhook/MCP/工作流服务回写真实状态。
3. **聚合更新**：飞书公式或工作流根据子任务、审核、阻塞计算需求健康度和下一门禁。

成员示例：

```text
更新 TASK-2026-042-07：
- 状态改为 In Review；
- 证据添加 GitHub PR #231；
- 已运行 unit 和 integration，链接为 [CI URL]；
- 残余风险：10 万行导出尚未完成性能测试；
- 下一动作 Owner 改为 Reviewer 陈涛，SLA 为明天 12:00。

只更新这条 Work Item，不修改需求总状态。
```

系统聚合后再决定 REQ 是否推进，避免个人误把整个需求标为完成。

### 4.3 每日自动摘要

每天 09:30 由飞书自动化或内部工作流生成成员视图：

- 今天到期、已逾期、等待我审核、由我解除阻塞。
- 不群发完整项目流水账；每人只收到与自己有关的行动项。
- TL/PM 收到团队级 Yellow/Red 项目、WIP 超限和门禁等待时长。

TRAE 摘要提示词：

```text
读取今天的 Requirements、Work Items 和 Approvals。
为每位成员生成最多 5 条可行动摘要：任务、原因、下一动作、截止时间、链接。
单独生成管理摘要：Red/Yellow、阻塞超过 24 小时、审核超过 8 工作小时、WIP 超限。
不要把无行动要求的信息列入通知。
```

## 5. 审核机制

### 5.1 风险决定审核深度

| 风险 | 条件示例 | 必需审核 |
|---|---|---|
| Low | 文案、小型内部工具 | Owner+1 Reviewer |
| Medium | 用户流程、普通 API | PM、TL、QA；代码 1 Reviewer |
| High | PII、支付、权限、数据迁移、生产基础设施 | PM、TL、QA、领域/安全；代码 2 Reviewer；Go/No-Go |

风险等级可以由规则给建议，但 TL 对技术风险、PM 对业务风险负责确认。

### 5.2 四层门禁

1. **内容门禁**：Skill 检查 PRD/TSD/测试/发布单字段是否完整。
2. **自动门禁**：CI、静态扫描、测试、制品和部署验证。
3. **同行审核**：对应角色在 Approvals 表作出决定。
4. **业务/发布决策**：PM 验收、TL/SRE Go/No-Go。

TRAE `Stop` Hook 适合第 1 层：如果智能体输出缺少 REQ-ID、版本、证据、风险或待确认项，就要求继续补齐。它不能替代第 2–4 层。

### 5.3 审核超时与升级

- PRD/TSD 普通审核 SLA：1 个工作日。
- PR Review：8 个工作小时；P0 修复：2 小时。
- 超过 50% SLA：私聊提醒 Approver。
- 到期：通知 Approver + DRI。
- 超期 4 小时：升级 TL/PM，由其改派或调整计划。
- 只提醒未决审核人，已批准者不再收到重复通知。

审核意见必须选择：`Approved`、`Changes requested`、`Rejected`；评论不能代替状态。

## 6. 知识和项目如何更新

### 6.1 区分项目事实与长期知识

- 项目事实：PRD、TSD、Figma、测试、发布单，随 REQ 生命周期更新。
- 长期知识：产品术语、架构规范、Runbook、踩坑经验，只有经过审核且具复用价值才进入企业文档集。

不要把每个聊天总结自动写进知识库，否则很快产生重复和冲突。

### 6.2 产物失效与重认证

上游批准版本变更时：

1. 依赖表找到所有下游 Artifacts。
2. 将其状态置为 `Stale`，但不自动覆盖正文。
3. 为各 Artifact Owner 创建“影响复核”任务。
4. Owner 选择 `No impact`、`Update required` 或 `Retire`，附原因。
5. 更新并重新审核后才恢复 `Approved`。

### 6.3 知识发布流程

```text
项目事件/复盘
  → Knowledge Candidate
  → 去重、脱敏、确认时效
  → 领域 Owner 审核
  → 更新飞书权威文档
  → 企业文档集定期重新索引
  → 记录发布日期、Owner、复核日期
```

建议每篇长期知识包含：适用范围、Owner、生效版本、最后复核、来源链接、废弃替代项。季度自动提醒 Owner 复核；超期标记 `Stale`，不立即删除。

## 7. 客户变更如何自动通知全部上下游

下面以客户要求在订单导出中增加“手机号”为例。

### 7.1 事件进入

销售或 PM 必须创建 `CR-2026-042-01`，而不是只发群消息。必填：来源、原话/证据、变化前后、原因、期望时间、关联 REQ-ID。

创建后自动执行：

1. 状态设为 `Assessing`，冻结受影响需求的下一门禁。
2. 通知 DRI，不立即广播全员。
3. 创建“变更澄清”和“影响分析”两个任务。
4. SLA 计时，例如 4 工作小时内给客户首次反馈。

### 7.2 TRAE 生成影响分析

DRI 在 TraeWork 运行：

```text
分析 CR-2026-042-01。
读取客户原始变更、REQ-2026-042、PRD/TSD/Figma 当前批准版本、Artifacts、Dependencies、Work Items 和未合并 PR。

请输出：
1. 变化前后和待澄清问题；
2. 受影响 FR-ID、产物、组件、任务、版本和人员；
3. 安全、隐私、权限、数据、容量、测试、发布和运营影响；
4. 必须重开的门禁；
5. 排期/成本区间与备选方案；
6. 精确通知名单，说明每个人为什么需要行动。

只生成评估草稿，不更新正式产物、不通知客户、不修改开发状态。
```

手机号属于 PII，规则应把风险升级为 High，并自动加入安全/隐私审核人。

### 7.3 精确通知，而不是 @所有人

影响确认后，工作流从依赖表计算：

- **Action required**：PM、设计、TL、后端、QA、安全；必须确认或完成任务。
- **Review required**：对应 Approvers；必须选择审核决定。
- **FYI**：销售、客户成功、SRE；仅接收摘要，不阻塞流程。
- **Not affected**：其他开发，不发送消息。

消息模板：

```text
[Action required] CR-2026-042-01 客户变更影响评估

变化：订单导出新增客户手机号
你被通知的原因：你是 order-service Owner，TSD/API/审计日志受影响
需要完成：2026-09-11 15:00 前确认影响并选择 No impact / Update required
当前基线：PRD v1.0、TSD v1.0、Figma v1.0
影响摘要：[链接]
你的任务：[TASK 链接]
逾期升级：TL 陈涛
```

通知必须包含原因、行动、截止时间和链接。只说“需求变更请关注”几乎等于没有通知。

### 7.4 收集确认和审核

每个受影响 Owner 在自己的任务上选择：

- `No impact`：说明为什么无需修改。
- `Update required`：列出产物、估算和依赖。
- `Need clarification`：明确问题和回答 Owner。

确认率达到 100%、澄清完成后，DRI 提交变更审批。High 风险至少需要 PM、TL、QA、安全批准；任何一人 `Changes requested`，门禁保持关闭。

### 7.5 批准后自动展开工作

变更批准事件触发：

1. PRD 从 v1.0 升 v1.1，追加 Change-ID。
2. 受影响 TSD/Figma/Test/Runbook 标记 `Stale`。
3. 从模板创建增量 Work Items，继承默认 Owner/Reviewer。
4. 未开始的旧任务更新输入版本；Doing 中的任务要求 Owner 选择继续、暂停或重做。
5. 未合并 PR 添加 `Change pending` 标签，禁止合并直到重新确认。
6. 重新计算关键路径、目标版本和风险。
7. 向 Action/Review/FYI 三组发送不同内容。
8. 给客户负责人生成可对外回复草稿；真人确认后发送。

### 7.6 拒绝或延期

- Rejected：记录原因和批准人，解除冻结，原基线继续有效。
- Deferred：关联候选版本和复核日期，不创建当前迭代任务。
- 不能删除变更记录；它是客户沟通和范围管理证据。

## 8. 自动化规则清单

| 触发事件 | 条件 | 自动动作 | 人工控制点 |
|---|---|---|---|
| 新需求创建 | 必填字段齐全 | 生成 REQ-ID、分诊任务、通知 DRI | 接受/拒绝/补充 |
| PRD 进入 Review | Reviewers 齐全 | 创建审核记录、启动 SLA | 各 Reviewer 决策 |
| PRD Approved | 全部必需审核通过 | 解锁设计/TSD，创建任务 | PM 最终确认 |
| 任务 In Review | 有产物链接 | 通知 Reviewer、读取 CI | Reviewer 批准 |
| CI 失败 | PR 未合并 | 状态 Yellow、通知 Owner | 修复或豁免审批 |
| 任务 Blocked | 超 24 小时 | 健康度 Red、升级 DRI/TL | 改派/调整范围 |
| 客户变更创建 | 关联已批准需求 | 冻结门禁、生成影响分析任务 | DRI 确认影响 |
| 变更 Approved | 确认率 100% | 标记 Stale、建增量任务、通知 | 各产物重新审核 |
| 发布成功 | Smoke 通过 | 进入 Observing、启动观察窗 | SRE/PM 关闭 |
| 知识到复核日 | 当前为 Published | 通知 Owner、标记待复核 | Owner 更新/废弃 |

## 9. TRAE、飞书、MCP、Hook、CI/CD 的职责边界

| 能力 | 适合承担 | 不应承担 |
|---|---|---|
| 飞书多维表格/自动化 | 主记录、任务、审核、SLA、人员通知 | 判断复杂技术影响 |
| TraeWork + 飞书集成 | 读写文档/表格、生成摘要、人工触发协作任务 | 无人监督地替人审批 |
| TRAE Skill | 标准化影响分析、分工、审核和输出 | 保存项目唯一状态 |
| TRAE MCP | 读取/操作 Git、CI、监控等系统 | 绕过目标系统权限 |
| TRAE Hook | AI 上下文、行为校验、审计、完成通知 | 监听客户/飞书业务事件 |
| CI/CD webhook | 构建、测试、制品、部署的真实状态 | 业务批准 |
| 内部工作流服务 | 跨系统编排、幂等、重试、事件日志 | 生成未经确认的业务决策 |

如果只使用 TRAE + 飞书内置能力，可以先实现“半自动”：事件自动建任务和通知，DRI 在 TraeWork 一键运行影响分析。若要做到跨 Git/CI/Figma 的全自动更新，需要一个轻量内部工作流服务接收事件、维护幂等和重试，并通过受控 API/MCP 联动各系统。

## 10. 自动化服务的可靠性要求

- 每个事件有唯一 `event_id`，重复投递不能重复建任务。
- 保留事件日志：来源、时间、旧值、新值、执行动作、结果、重试次数。
- 通知失败可重试；审批、生产发布和客户发送不可自动重试造成重复副作用。
- 工作流失败时将需求健康度标 Red，并给 DRI 建人工补偿任务。
- 不把 App Secret、Token 或客户敏感内容写入 TRAE Prompt、表格通知和日志。
- 所有自动写入都记录“系统执行者”和对应规则版本。

## 11. 四周落地顺序

### 第 1 周：可见

建立 6 张表、REQ/TASK/CR 编号、Owner/Reviewer、状态机和个人视图。先人工操作，不急着全自动。

### 第 2 周：可控

上线审核表、状态迁移规则、SLA 提醒、Blocked 升级和每日个人摘要。

### 第 3 周：可追溯

打通 PR/CI/制品链接，建立 Artifacts 与 Dependencies，客户变更能精确计算通知名单。

### 第 4 周：自动化

上线变更冻结、Stale 标记、增量任务创建、知识复核提醒；TRAE Hook 先审计后拦截。

首月验收标准：

- 100% 需求、任务、变更有唯一 ID 和单一 Owner。
- 状态 Done 的任务 100% 有验收证据。
- 客户变更 30 分钟内通知 DRI，4 工作小时内完成初步影响评估。
- 受影响 Owner 的确认率可见，未确认者能按 SLA 升级。
- 不再依赖群聊搜索来判断当前需求、版本和决定。

## 12. 官方能力依据

- [TraeWork 飞书集成](https://docs.trae.cn/work_feishu-integration)：可在授权范围内读取、创建和更新云文档、多维表格、任务、消息等。
- [企业文档集](https://docs.trae.cn/enterprise_enterprise-doc-set)：飞书文档可被持续索引并通过 `#Doc` 使用。
- [TRAE 企业 Hook](https://docs.trae.cn/enterprise_automate-actions-with-hooks)：用于智能体执行过程中的上下文、校验、审计、拦截和通知。
- [企业 Hook 配置详解](https://docs.trae.cn/enterprise_hook-configuration-reference)：`Stop` 可检查产出，`Notification` 为非阻断完成/等待通知。
- [TraeWork GitHub 集成](https://docs.trae.cn/work_github-integration)：支持创建和检查 PR。
