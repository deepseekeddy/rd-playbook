# TRAE 旗舰版十人团队研发全流程设计

> 目标：用“一个需求主记录 + 一条可审计状态流 + 一组质量门禁 + 一套企业 Skill”贯通需求、PRD、设计、TSD、开发、测试、发布、CI/CD 与运营。

## 1. 设计结论

TRAE 不应替代飞书、Figma、Git 和 CI/CD，而应成为跨系统的 AI 执行层。建议分工：

- **飞书**：需求主记录、PRD/TSD、决策、会议纪要与发布公告。
- **Figma**：交互与视觉唯一事实源；飞书 PRD 只保留链接、版本和验收摘要。
- **Git 平台**：代码、分支、Issue/PR、评审记录和可执行配置唯一事实源。
- **CI/CD 平台**：构建、测试、制品、部署、回滚和环境状态唯一事实源。
- **TRAE**：借助企业文档集、企业 Skill、企业智能体、MCP 和 Hook，读取上下文、生成产物、执行检查并回写证据。

避免把所有内容复制进一个“大知识库”。每类信息只设一个权威来源，其他系统保存 ID、链接、版本和状态。

## 2. 十人团队建议角色

| 角色 | 建议人数 | 核心责任 |
|---|---:|---|
| 产品负责人/PM | 1 | 需求价值、范围、PRD、验收口径 |
| 设计 | 1 | 交互、视觉、组件复用、设计验收 |
| 技术负责人/TL | 1 | 架构、TSD、风险、技术门禁、发布决策 |
| 开发 | 5 | 任务拆分、实现、自测、PR、可观测性 |
| QA | 1 | 测试策略、风险测试、验收证据 |
| DevOps/SRE | 1（可由开发兼任） | 流水线、环境、发布、监控、回滚 |

每个需求设置一个 **DRI**。状态推进由 DRI 负责，门禁批准由对应 Owner 负责；AI 只提供建议和证据，不代替业务、架构、安全和发布责任人签字。

## 3. 端到端状态机

`Idea → Triaged → PRD Draft → PRD Approved → Design Ready → TSD Approved → Ready for Dev → In Dev → In Test → Ready for Release → Released → Observing → Closed`

异常状态：`Blocked`、`Cancelled`、`Rolled Back`。任何回退都要记录原因、责任人和下一步。

| 阶段 | 必备输入 | 标准产物 | 出口门禁 | Owner |
|---|---|---|---|---|
| 接需求 | 原始反馈/目标 | 需求单、价值/优先级、DRI | 问题、用户、指标、范围明确 | PM |
| PRD | 已分诊需求 | PRD、验收标准、埋点草案 | 产品/设计/TL/QA 通过 | PM |
| 设计 | PRD | Figma、状态矩阵、组件映射 | 关键流、异常态、响应式、可访问性齐备 | 设计 |
| TSD | PRD+设计+代码上下文 | TSD、接口/数据/迁移/回滚方案 | TL+相关开发+QA 通过 | TL |
| 计划 | 已批准 TSD | Issue/WBS、依赖、估时、测试任务 | 每项可独立验收且可追踪 | TL/DRI |
| 开发 | Issue+TSD | 代码、单测、文档、迁移脚本 | 本地检查通过、PR 证据完整 | 开发 |
| 评审 | PR | Review 结论、修复记录 | 2 人规则/高风险专项通过 | Reviewer/TL |
| 测试 | 候选制品 | 测试报告、缺陷、验收证据 | P0/P1 清零，回归/非功能达标 | QA |
| 发布 | 已签字候选 | Release note、变更单、制品、回滚包 | 四眼审批、备份/回滚/监控就绪 | TL/SRE |
| 运营 | 线上版本 | 指标看板、告警、复盘、改进项 | 观察窗无异常、目标指标已评估 | PM/SRE |

## 4. 需求主记录数据模型

在飞书多维表格中为每项工作建立唯一 `REQ-ID`，建议字段：

- 身份：`REQ-ID`、标题、类型、提出人、DRI、参与人、创建时间。
- 价值：用户/场景、问题、业务目标、成功指标、优先级、成本等级。
- 范围：In scope、Out of scope、依赖、风险、目标版本。
- 状态：当前阶段、阻塞原因、下一个门禁、到期日、最后更新时间。
- 关联：PRD、Figma、TSD、Issue/Epic、PR、流水线、制品、测试报告、发布单、监控看板、复盘。
- 决策：决策日志、批准人、批准时间、变更摘要。

所有下游产物标题包含 `REQ-ID`；Commit/PR/流水线元数据也携带它，实现从需求到部署的双向追溯。

## 5. 协作机制

具体到 TraeWork、TraeCode、飞书和 Figma 的配置入口、日常操作与完整示例，见 [TRAE协作机制操作手册](TRAE协作机制操作手册.md)。

团队任务分配、状态同步、审核门禁、知识更新及客户变更自动通知机制，见 [团队调度与变更自动化设计](团队调度与变更自动化设计.md)。

### 单一事实源与版本

- PRD/TSD 使用飞书文档，正文首部固定：状态、Owner、评审人、版本、最后更新、关联 REQ-ID。
- Figma 页面按 `00-Cover / 10-Flow / 20-Screens / 30-Components / 40-Prototype / 90-Archive` 组织；交付时冻结版本链接。
- 代码变更必须通过 PR；禁止把聊天结论当成正式决策，重要结论回写决策日志。
- 需求变更在 PRD Approved 后必须走轻量变更控制：变化、原因、影响、是否重做设计/TSD/测试、批准人。

### 会议最小化

- 每日 10 分钟：阻塞、当天目标、风险，不逐人汇报流水账。
- 每周需求分诊 30 分钟：只决定接受/拒绝/补充信息/优先级。
- PRD/TSD 异步评审优先；只对争议点开 30 分钟决策会。
- 发布前 15 分钟 Go/No-Go；发布后按风险设 30 分钟至 24 小时观察窗。
- 双周复盘 Skill/流程：统计门禁失败、返工、漏测、AI 产出采纳率。

## 6. TRAE 架构

### 文档集

建议按权限与更新频率拆为 5 个企业文档集：

1. `RD-Policy`：研发流程、DoR/DoD、分支/评审/发布/安全规则。
2. `Product-Knowledge`：产品术语、用户画像、业务规则、指标口径。
3. `Architecture`：系统边界、ADR、接口规范、数据规范、技术栈。
4. `Quality`：测试策略、缺陷分级、质量基线、上线验收。
5. `Operations`：SLO、监控、告警、应急预案、Runbook。

飞书同步适合持续维护的纯文本知识；图片和附件不能被索引，因此 Figma/架构图必须配文字说明。每个文档集设 Owner、可见范围、季度复核和过期标记。

### Skill、智能体、MCP、Hook 的边界

| 能力 | 用途 | 本方案中的位置 |
|---|---|---|
| Skill | 固化单个专业流程和输出标准 | 本目录 11 个 Skill |
| 企业智能体 | 编排角色、模型、工具与多个 Skill | 产品助手、架构助手、研发助手、QA 助手、发布助手 |
| MCP | 对接 Git/需求/CI/CD/监控等外部系统 | 只开放必需工具，按环境最小权限 |
| Hook | 强制检查、审计、拦截、上下文注入 | 会话注入、危险命令阻断、停止前验收、异步通知 |
| Command | 固定入口和快捷操作 | `/intake`、`/prd`、`/tsd`、`/review`、`/release-check` |

### 五个企业智能体

- **Product Agent**：需求澄清、PRD、变更影响；只读产品文档集，可写需求系统草稿。
- **Architecture Agent**：TSD、ADR、接口/数据评审；读代码与架构文档，不直接部署。
- **Developer Agent**：任务拆分、实现、单测、PR；写工作区，生产环境无权限。
- **QA Agent**：测试策略、用例、回归、缺陷复现；可触发测试流水线，不可发布。
- **Release Agent**：发布检查、变更说明、观测与回滚建议；生产写操作必须人工确认。

## 7. CI/CD 参考流水线

`pre-commit → PR validate → build → unit → static/security → integration → preview → QA/UAT → package/sign/SBOM → staging → approval → canary → production → smoke → observe → auto/one-click rollback`

最低门禁：

- PR：格式、lint、类型、单测、依赖与密钥扫描、变更影响、至少 1 名 Reviewer；核心模块 2 名。
- 合并：主干保护、检查全部通过、不得直接 push、PR 与 REQ-ID 关联。
- 发布：不可变制品跨环境晋级；数据库迁移兼容旧版本；配置与代码分离；回滚已演练。
- 生产：灰度/金丝雀、健康检查、错误率/延迟/业务指标门禁、自动暂停或回滚。

## 8. 企业 Hook 设计

| 事件 | 建议动作 | 失败策略 |
|---|---|---|
| `SessionStart` | 注入项目、环境、REQ-ID、分支规则和文档集指引 | 非关键上下文可降级 |
| `UserPromptSubmit` | 检查是否包含敏感数据、生产操作意图和需求 ID | 高风险阻断并提示补充审批 |
| `PreToolUse` | 拦截生产写入、删除、密钥读取；校验允许目录/命令/MCP | 默认拒绝高风险操作 |
| `PostToolUse` | 记录变更与测试证据；对修改触发 lint/test 建议 | 审计失败告警，关键证据失败阻断 |
| `Stop` | 检查目标、产物、测试、未决风险和链接是否完整 | 不达标时要求智能体继续补齐 |
| `Notification` | 完成、失败、等待批准时通知 DRI | 异步，不阻塞主流程 |

阻塞型 Hook 必须高可用、短超时并有监控；先在非生产团队灰度，否则 Hook 服务异常会阻断成员问答。

## 9. Skill 库治理

本目录采用“一个节点一个 Skill + 一个总编排 Skill”。每个 Skill 必须声明：

- 精准触发描述；所需输入与缺失信息处理；步骤；固定输出；完成定义；停止/升级条件。
- 不得伪造链接、执行结果、测试结果、批准或业务数据。
- 输出必须带 `REQ-ID`、版本、Owner、风险、未决项和证据链接。

发布流程：提案 → Owner 编写 → 2 个真实样例验证 → 同行评审 → 管理员发布企业 Skill → 两周灰度 → 指标复盘 → 正式版。版本使用 SemVer；破坏性变更升主版本。每个 Skill 设业务 Owner 和技术 Maintainer。

建议 KPI：触发准确率、一次通过率、人工编辑率、返工率、缺陷逃逸率、Lead Time、部署频率、变更失败率、MTTR。不要用代码生成行数评价个人。

## 10. 30/60/90 天落地

### 0–30 天：建立骨架

- 统一 REQ-ID、状态机、PRD/TSD/测试/发布模板和 DoR/DoD。
- 打通飞书文档集；上线需求、PRD、TSD、代码评审、发布检查 5 个高价值 Skill。
- 建立主干保护与基础 CI；选一个中等风险需求做试点。

### 31–60 天：接入工具链

- 配置 Git、需求平台、CI/CD、监控 MCP；设置白名单与最小权限。
- 建立企业智能体；上线 Hook 灰度，先审计后拦截。
- 实现 REQ-ID 在 Issue/PR/流水线/发布记录间自动校验。

### 61–90 天：量化与优化

- 上线灰度、自动回滚、SLO 和 DORA/质量看板。
- 用真实失败案例迭代 Skill；下线重复、低触发和低收益 Skill。
- 完成灾备/回滚演练、安全审查与团队认证。

## 11. 首个试点验收标准

- 一个需求可从 REQ-ID 跳转到 PRD、Figma、TSD、Issue、PR、测试、制品、发布与监控。
- 每个阶段都有明确 Owner、入口、出口和证据；状态不能仅靠口头更新。
- CI 失败不可合并；未经批准不可生产发布；能在约定时间内回滚。
- 5 个核心 Skill 在真实任务上触发正确率 ≥ 90%，关键字段遗漏率 < 5%。
- 试点结束能给出 Lead Time、评审等待时间、返工原因和线上质量结果。

## 12. 当前待确认项

- 飞书培训表正文当前受登录限制，需进一步映射其中的课程节点、角色和模板字段。
- Git 平台、CI/CD 平台、云环境、监控栈、分支策略和发布频率尚未提供，本方案按平台无关方式设计。
- 是否采用 Scrum/Kanban、是否有合规审批、生产发布权限边界，需要在试点启动会上定稿。
